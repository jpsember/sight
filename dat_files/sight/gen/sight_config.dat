fields {
  int done_pause_time_ms = 1200;
  int quiescent_chord_ms = 250;
  bool view_recent_edits;
  bool create_chords;
  enum Hand hand;
  enum KeySig key;
  bool inspect_boxes;
  int seed;

  // For practice mode, include only lessons whose descriptions
  // match this regex

  string pattern;

  int resolution = 160;

  bool silent_correction;
}
